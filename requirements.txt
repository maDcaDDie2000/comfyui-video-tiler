# ComfyUI Video Tiler - No extra dependencies required
# Uses PyTorch + NumPy (already provided by ComfyUI)
#
# Video Tile Audio Present: install FFmpeg and ensure `ffprobe` is on PATH when probing files from AUDIO metadata.
